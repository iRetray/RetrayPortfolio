type CommandConsoleProps = {
  command: string;
};

const CommandConsole = ({ command }: CommandConsoleProps): JSX.Element => (
  <div>Write some commands here</div>
);

export default CommandConsole;
